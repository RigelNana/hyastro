---
status: accepted
---

# 采用混合强类型领域模型与模块错误

hyastro 对常用时间尺度、参考系统、原点、物理量和位置阶段采用密封静态类型，使错误组合在编译期失败；具体参考架实现和动态 SPICE 帧采用运行时受检值，以控制泛型规模并支持外部数据。公开类型全部由 hyastro 拥有，上游类型只存在于适配器实现内。结果只保存数值和继续计算所需的强类型语义。每个深模块拥有独立且非穷尽的错误枚举，高层错误保留下层来源。完整类型与错误契约见 `docs/DOMAIN_MODEL.md`。

# hyastro 天文计算领域词汇表

本文件只定义领域语言。算法、依赖和工程决策记录在 `docs/` 文档中。

## 核心量

### 瞬间（Instant）
物理时间线上的一个点。瞬间本身不等于某个历法日期，也不等于某个时间尺度下的数值表示。

### 时长（Duration）
两个瞬间之间的物理间隔。历月和历年不是固定时长。

### 时间尺度（Time Scale）
为瞬间赋予读数的约定，例如 UTC、TAI、TT、UT1、TCG、TCB、TDB。时间尺度不是时区。

### 时区（Time Zone）
民用本地时间与 UTC 的地区性映射规则。时区不是时间尺度。

### 历法（Calendar）
把日期映射到日序的规则，例如公历和儒略历。历法不决定时间尺度。

### 日期（Date）
历法中的年、月、日三元组，不含时刻或时间尺度。

### 日期时间（DateTime）
某个历法、时间尺度和可选时区规则下的日期与日内时刻表示。

### 固定 UTC 偏移（Fixed UTC Offset）
相对 UTC 恒定不变的有符号时间偏移。它不包含夏令时、历史变更或地域时区规则，不能与时区等同，也不是天文学中的时差（Equation of Time）。

### 固定偏移民用时间（Fixed-Offset Civil Date-Time）
由历法日期、日内时刻和固定 UTC 偏移组成的民用标签。其公历年份归属按该固定偏移判断，而不是按 UTC 日期判断。

### 闭时间区间（Closed Time Interval）
同时包含起点和终点的非空物理时间区间；两个端点必须属于同一类型化时间尺度，且起点严格早于终点。

### 儒略日（JD）
以日为单位连续计数的时间表示。JD 必须与时间尺度一起解释；裸 `JD` 不足以唯一表达瞬间。

### 修正儒略日（MJD）
定义为 `JD - 2_400_000.5`。MJD 同样必须带时间尺度语义。

### 两段式日期（Two-Part Date）
用两个浮点数之和表示 JD 或等价日数，以保留大基数附近的小增量精度；两段的拆分方式不改变所表示的瞬间。

### 历元（Epoch）
时间轴上的参考瞬间，供坐标、轨道根数或星表参数引用。历元必须说明时间尺度和表示。

### 儒略纪元（Julian Epoch）
以 365.25 日的儒略年相对于 J2000.0 定义的连续历元表示，例如 J2016.0；不是公历年份。

### 贝塞尔纪元（Besselian Epoch）
按传统贝塞尔年定义的连续历元表示，例如 B1950.0；不是儒略纪元。

### 历年（Calendar Year）
由历法规则划分的年份标签区间。公历年通常为 365 或 366 个民用日，儒略历年也随闰年变化；“自然年”含义不稳定，公开接口必须改用明确的历法和起止日期。

### 历月（Calendar Month）
由历法中的年、月标签确定的日期区间；长度随历法、年份和月份变化。历月不是固定秒数，也不是任何一种月球轨道周期。

### 历法跨度（Calendar Span）
以整历年、整历月和整日表达的日期位移。它必须携带月底无对应日期时的夹取或拒绝策略，不能与物理 `Duration` 相互隐式转换。

### 儒略年（Julian Year）
精确定义为 365.25 个 86,400 SI 秒的儒略日，供历元、自行率和单位换算使用。它不是公历年，也不是观测得到的回归年或恒星年。

### 回归年（Tropical Year）
太阳平均黄经相对于移动分点增加一整圈所定义的天文周期。其长度随历元和所选平均模型缓慢变化；连续两次实际春分之间的分点年是相关但不同的事件间隔。

### 恒星年（Sidereal Year）
地球相对固定惯性方向绕太阳完成一周的周期。它依赖参考方向、历表和测量区间，不得用固定的儒略年代替。

### 近点年（Anomalistic Year）
地球连续两次通过近日点之间的物理间隔。近日点受摄动和进动影响，因此单次近点年不是永恒常数。

### 交点年（Draconic Year）
太阳相对同一月球轨道交点完成一周的周期，也称食年。它不同于公历年、回归年和恒星年，是食季重复关系的基础周期之一。

### 朔望月（Synodic Month）
月球相对太阳的同一有向相位再次出现所经历的周期；朔到下一朔称一个朔望月或朔望周期。单次周期由两次物理事件之差得到，平均值不能替代实际事件。

### 恒星月（Sidereal Month）
月球相对固定惯性方向绕地球完成一周的周期。

### 回归月（Tropical Month）
月球黄经相对于移动分点增加一整圈的周期；它不同于朔望月，也因分点进动而与恒星月略有差异。

### 近点月（Anomalistic Month）
月球连续两次通过近地点之间的物理间隔。

### 交点月（Draconic Month）
月球连续两次以同一方向通过同一轨道交点之间的物理间隔，也称龙月或节点月。

### 闰秒（Leap Second）
UTC 中为控制 UT1 与 UTC 差异而插入或删除的标记秒。闰秒属于 UTC 表示规则，不属于 TAI 时间线。

### 地球定向参数（EOP）
连接观测地球姿态与约定模型的数据集合，至少包括 UT1−UTC、极移 `xp`/`yp`、天极偏差 `dX`/`dY`，并可包括 LOD 及其不确定度。

### Delta T
`TT − UT1`。它不是 `TAI − UTC`，也不是 `UT1 − UTC`。

### TDB−TT
重心动力学时与地球时在同一物理瞬间的坐标差。它以周期相对论项为主，并随所选模型以及观测者取地心还是站心而变化；不能作为固定偏移处理。

### 平太阳时（Mean Solar Time）
由匀速运动的假想平太阳定义的地方日内时刻。格林尼治平太阳时以 UT1 表示；其他经度的地方平太阳时还包含该经度相对格林尼治的恒定偏移。平太阳时不是 UTC、时区或民用时间。

### 真太阳时（Apparent Solar Time）
由真太阳的当地时角定义的地方日内时刻，等于真太阳当地时角加 12 小时。它随经度和太阳实际视位置变化，不是匀速时间尺度。

### 时差（Equation of Time）
同一经度、同一瞬间的真太阳时减去平太阳时。正时差表示真太阳时领先；经度对两者施加相同偏移，因此时差本身与经度无关。时差不同于分点差、原点方程、时区偏移和 `UT1−UTC`。

## 模型与结果语义

### 模型（Model）
定义计算所采用的物理约定、数学公式和参数集合。模型必须以可识别的标准、版本和适用范围区分。

### 覆盖区间（Coverage）
数据或模型保证可用的时间、空间或目标范围。覆盖之外的结果只能由显式外推策略产生。

### 修正（Correction）
为把位置、方向、状态或时刻从一种明确语义变换为另一种语义而应用的已命名物理效应或约定项，例如光行差、引力偏折、极移或折射。修正前后的结果必须具有可区分语义。

## 光度

### 光度通带（Photometric Passband）
仪器对波长的相对响应函数。通带不是单一中心波长；同名滤光片的不同响应曲线或版本也不能静默视为同一通带。

### 星等系统（Magnitude System）
把通带内接收通量映射为星等的零点约定，例如 Vega、AB 或 ST。星等系统不是通带；一个星等值必须同时绑定通带和零点约定才可比较或换算绝对通量。

### 视星等（Apparent Magnitude）
观测者处某一通带和星等系统中的对数亮度。视星等可为负值；必须说明观测者几何、采用的光度模型以及是否已施加本地大气消光。

### 星等差与通量比（Magnitude Difference and Flux Ratio）
同一通带和星等系统中两个源的星等差与正通量比满足 `m₂ − m₁ = −2.5 log₁₀(F₂/F₁)`。只有通带和零点约定相容的星等才可直接相减；通量比不同于带物理单位的绝对通量。

### 太阳系参考星等（Solar-System Reference Magnitude）
反射太阳光目标在日心距和观测者距均为 1 AU、物理相位角为零时的模型参考星等，常写作 `V(1,0)`。它不同于把恒星置于 10 pc 定义的恒星绝对星等，也不能与小天体 `H` 参数静默互换。

### 恒星绝对星等（Stellar Absolute Magnitude）
恒星在无消光条件下置于 10 pc 时，在指定光度通带和星等系统中的视星等。它必须与实际视星等、太阳系参考星等和小天体绝对星等使用不同语义。

### 星表平均光度（Catalog Mean Photometry）
星表在一个观测时间窗和数据处理版本内由多次观测汇总得到的平均通量、平均星等及质量信息。它不是任意请求时刻的瞬时星等；对变星、食双星和爆发源尤其不能如此解释。

### 色指数（Color Index）
同一星等系统中两个已命名且版本化通带星等的有向差，例如 `G_BP − G_RP`。色指数的通带顺序属于量的语义，不能退化为无标签星等差。

### 星际消光（Interstellar Extinction）
源光在到达太阳系前被星际尘埃吸收和散射造成的通带星等增量。它依赖消光定律、尘埃参数、源光谱和通带，不能与地球本地大气消光合并成一个无来源修正。

### 合成光度（Synthetic Photometry）
把版本化源光谱能量分布与完整通带响应及零点标定组合得到的模型星等或通量。单一有效温度或黑体曲线不足以作为一般恒星合成光度模型。

## 空间与运动

### 点（Point）
相对于一个原点、在一个参考系中表达的位置。点可平移；不能把点当作自由向量。

### 向量（Vector）
带方向和大小的几何量。其分量必须依附参考系，物理量必须依附单位。

### 状态（State）
同一历元、原点和参考系下的位置与速度二元组。

### 方向（Direction）
单位球面上的无距离射线。方向不同于三维位置，也不同于带距离的球面坐标；它只绑定轴向约定，从位置或状态求方向时仍必须给出观察原点。

### 旋转（Rotation）
从一个参考系到另一个参考系的定向变换。矩阵和单位四元数是旋转的表示，不是不同的物理变换。

### 状态变换（State Transform）
同时变换位置与速度的六维变换；当参考系随时间旋转时，它不等于把同一个三维旋转分别应用到位置和速度。

### 球面坐标（Spherical Coordinates）
由经度型角、纬度型角和可选距离表达的坐标。不同领域坐标的范围和零点不同，不能只靠三个裸浮点数区分。

### 角位置（Sky Position）
某个天球参考系中的方向表示，通常由赤经/赤纬或经度/纬度组成。

### 自行（Proper Motion）
星表源在参考历元处的切平面角速度。`μα* = μα cos δ` 与未乘 `cos δ` 的 `μα` 是不同量。

### 径向速度（Radial Velocity）
沿视线方向的速度。必须声明符号约定和所采用的测量定义；光学、射电和相对论定义不能混用。`CatalogRadialVelocity` 专指太阳系质心处的天体测量径向速度，正值表示退行，不等同于未经转换的光谱红移速度。

### 视差测量（Parallax Measurement）
星表拟合得到的有符号周年视差及其非负标准不确定度。噪声可能产生零或负测量值；它们是有效测量结果，但不是可直接反演的物理距离。

### 物理视差（Physical Parallax）
由观测基线引起的正且有限的视方向变化，并可作为距离参数。`ParallaxMeasurement` 只有在中心值严格为正且满足所选解释策略时才能转为 `Parallax`；不得静默截零、取绝对值或制造负距离。

### 标准不确定度（Standard Uncertainty）
与一个量相同维度的非负一倍标准不确定度。`StandardUncertainty<Q>` 保留量纲和规范单位；单独一个标准不确定度不等于完整误差预算，不说明误差是否独立、Gaussian，也不包含未显式给出的系统误差、模型差异或协方差。

### 协方差（Covariance）
参数估计误差及相关性的矩阵表示。传播星表参数时，协方差与参数必须使用相同顺序、单位和切平面约定。

### 星表参数协方差（Catalog-Parameter Covariance）
有限距离六参数星表解中参数估计误差的完整协方差。规范顺序为局部赤经切平面分量 $\alpha*$、赤纬 $\delta$、周年视差 $\varpi$、赤经自行 $\mu_{\alpha *}$、赤纬自行 $\mu_\delta$、质心天体测量径向速度 $v_r$。$\alpha*$ 表示微分 $d\alpha\cos\delta$，不是全局赤经坐标；协方差跨历元传播时，输入和输出必须各自在所属历元的局部切平面解释。

## 参考系统、参考架和原点

### 参考系统（Reference System）
坐标定义所依据的理想化约定、模型和物理原则，例如 ICRS 或 ITRS。

### 参考架（Reference Frame）
参考系统的具体实现，例如某一版 ICRF 或 ITRF。系统与架不能互换命名。
### 计算坐标框架（Coordinate Frame）
为几何分量提供完整计算语义的类型标签，绑定参考系统或其具体实现、原点、轴定义和手性。它是 hyastro 的计算模型，不是“参考架”的同义词；例如 `Bcrs` 与 `Gcrs` 是不同的计算坐标框架，而 ICRF3、ITRF2020 是参考架实现。


### 原点（Origin）
位置和状态的零点，例如太阳系质心、地心、站心或天体质心。

### ICRS
国际天球参考系统，太阳系质心、准惯性；其实现由 ICRF 及与之对齐的星表给出。

### BCRS
太阳系质心天球参考系统，是相对论时空坐标系统；与 ICRS 的轴向约定紧密相关，但概念不同。

### GCRS
地心天球参考系统，是地心相对论时空坐标系统。

### CIRS
天球中间参考系统，以 CIP 和 CIO 定向；其坐标为中间赤经与中间赤纬。

### TIRS
地球中间参考系统，由 ERA 围绕 CIP 从 CIRS 旋转得到，尚未应用极移。

### ITRS
国际地球参考系统，地固坐标系统；实际坐标由具体 ITRF 实现和站点运动模型给出。

### CIO
天球中间原点，定义 CIRS 的赤经零点。

### TIO
地球中间原点，定义 TIRS 的经度零点。

### CIP
天球中间极，其 GCRS 坐标常以 `X`、`Y` 表示。

### 赤道坐标
以指定赤道和赤经零点定义的方向坐标族。必须进一步说明 ICRS、CIRS、日期平/真赤道与分点或 FK4/FK5；“赤道坐标”本身不唯一。

### 黄道坐标
以指定黄道模型和黄经零点定义的方向坐标族；“J2000 平黄道”“日期平黄道”和“日期真黄道”不是同一轴向约定。项目中的日期真黄道采用明确的 Astropy/ERFA 惯例：IAU 2006 frame bias/岁差、IAU 2000A 章动与真黄赤交角 $\epsilon_A+\Delta\epsilon$ 共同定义日期真黄道面和真分点。黄道轴本身不指定地心、日心或太阳系质心原点，也不表示已经应用光行时、光行差、视差或引力偏折；带距离的位置及视位置必须另行建模。

### 银道坐标
IAU 1958 银道系统的方向坐标，以银经和银纬表示。现代 ICRS 转换采用 Hipparcos 给出的规范直接实现；它不同于未修正 E-terms、自行系统和 B1950 分点影响的原始 FK4 表示。带距离的日心或银心位置不是同一个模型。

### 旋转椭球（Rotational Ellipsoid）
由长半轴与扁率定义的轴对称参考面。参数集必须绑定可识别的模型与版本；它不是实际地形或大地水准面。

### 测地经度（Geodetic Longitude）
参考椭球上法线子午面相对本初子午面的东正夹角。它必须绑定参考椭球与地固参考系统，不等同于任意球面或天球经度。

### 测地纬度（Geodetic Latitude）
参考椭球法线与赤道面的夹角，不等同于地心位置向量的纬度。

### 地心纬度（Geocentric Latitude）
地心位置向量与赤道面的夹角。除球形近似外通常不等于测地纬度；在地心原点没有定义。

### 椭球高（Ellipsoid Height）
沿参考椭球法线、相对椭球面的有符号高度，不等同于相对大地水准面的正高。

### 固定站点（Fixed Site）
在指定地固参考系统中位置恒定的观测者定义。固定只描述该模型内不含站点运动；不自动声称某个 ITRF 实现、参考历元、板块运动或载荷改正。

### 历元固定观测者（Fixed Observer At）
一个固定站点在单一接收历元的天体测量上下文。它组合站点 GCRS 状态、地球 BCRS 历表状态和星无关光行差参数，可供同站点同历元的多个有限目标复用。站点速度模型必须可查询：完整 EOP 路径使用观测 LOD 和可用帧率，姿态表降级路径必须显式声明 IERS 名义地球自转率；它不是新的天体或可跨历元复用的静态原点。

### 本地切向基（Local Tangent Basis）
由站点参考椭球法线和子午面定义的局部正交方向。ENU 按东、北、上排列，NED 按北、东、下排列；使用时必须说明这些方向表达在哪个参考架及其有效历元。

### 站心参考架快照（Topocentric Frame Snapshot）
固定站点在一个物理历元的 GCRS 位置/速度和局部 ENU 基。站点、历元、地球姿态求值和 `SiteVelocityModel` 不可拆分；它不等同于仅有轴定义的静态参考系标记。

### 地平坐标
观测者局部参考系中的方位和高度/天顶距。hyastro 的真空地平方向采用北为 $0^\circ$、向东增加；天顶和天底的方位无定义。结果必须说明站点、历元和是否含折射。

## 天体测量结果

### 星表位置（Catalog Place）
星表在参考历元给出的参数化位置，未自动包含观测时刻的传播和观测效应。`InfiniteCatalogPlace` 是受限星表位置：只接受 ICRS 赤经/赤纬、TCB 参考历元以及显式采用 $\mu_{\alpha *}=\dot{\alpha}\cos\delta$ 约定的二维自行；零视差和零径向速度由类型表达，而不是以缺失字段或魔数暗示。`SpatialCatalogPlace` 是物理有限距离的六参数星表位置，额外要求正 `Parallax` 和正值表示退行的 `CatalogRadialVelocity`。

### 质心星表状态（Barycentric Catalog State）
以 SSB 为原点、与 ICRS 对齐的洛伦兹参考架中的三维位置和速度，并绑定一个 TCB 参考历元。它是六参数星表位置与直线空间运动传播之间的状态表示，不是太阳系天体的历表状态。

### 天体测量位置（Astrometric Place）
经空间运动和光行时等几何/相对论处理、但按指定约定尚未加入全部观测者相关效应的位置。具体修正集合必须由结果元数据声明。`AstrometricCatalogPlace<S, C>` 是星表源在观测时刻传播后的 SSB/ICRS 方向；站点位移造成的 Roemer 调制及有限距离视差仍属于后续观测者链。

### 视位置（Apparent Place）
按指定模型加入岁差章动、光行差和引力偏折等效应后，与选定天球中间/真参考系一致的位置。该术语必须绑定模型与参考系。

### 观测位置（Observed Place）
由真空观测位置显式应用调用者提供的大气条件后，在观测者局部系统中的折射后位置。该阶段保存来源真空结果，不能再次应用折射。

### 真空观测位置（Vacuum Observed Place）

观测者局部系统中的站心方向和可选距离，已应用结果所声明的光传播、地球定向与站心修正，但明确未应用大气折射或其他传播介质修正。它是观测位置的受限语义，不等同于包含折射的观测位置。

### 大气条件（Atmospheric Conditions）

一次观测的气压、温度、相对湿度和观测波长组合。它们是显式、带单位、经模型范围验证的动态输入，不属于固定站点身份；零气压明确表示真空。

### 折射修正（Refraction Correction）

真空天顶距减去观测天顶距的有符号角量，并携带所用模型及其在当前天顶距的适用范围分类。它不是气象测量误差或统计不确定度。

### 球形天体形状（Spherical Body Figure）

把一个有物理表面的天体、正半径和版本化模型标识绑定为不可拆分的球形表面模型。系统质心没有物理表面，不能拥有天体形状。

### 视半径（Apparent Semidiameter）

从观测者看，天体中心方向到其视边缘的角距离。球形模型使用目标发射历元到观测者接收历元之间经光行时收敛的站心距离；它不是小角近似，也不等于折射后的垂直半径。

### 真空视盘（Vacuum Apparent Disk）

由真空观测位置和同一目标的球形天体形状共同定义的圆形视盘。它保存中心位置、视半径、视直径和形状模型；大气差分折射造成的盘面压缩属于另一个结果，不能覆盖真空视盘。

### 几何状态（Geometric State）
同一坐标时刻上的目标与观测者状态差，未加入光行时、光行差或引力传播修正。

### 接收光行时结果（Reception Light-Time Result）
目标位置在发射时刻、观测者位置在接收时刻求值后形成的相对位置。结果必须同时保存两个物理历元、单程光行时、距离、自然视线方向、迭代次数和时间残差；不同历元的速度不能相减后伪装成一个 `RelativeState`。当前接收链不包含 Shapiro 延迟。

### Roemer 延迟
因观测者相对参考原点的几何位置而产生的光程时间差。

### 引力偏折
光线经过引力体时的方向改变。太阳偏折是常见特例，多体偏折是更一般形式。

### 光行差（Aberration）
由观测者运动引起的视方向变化。周年光行差与周日光行差对应不同运动来源。

### 太阳地心视黄经（Geocentric Apparent Solar Longitude）
从地心在接收时刻观测太阳所得的日期真黄道经度。它必须同时声明所用视位置修正集合、黄道轴模型和求值历元。

### 折射（Refraction）
地球大气使光学视线弯曲的修正。折射模型和输入气象条件属于结果语义的一部分。

## 历表与事件

### 历表（Ephemeris）
按时间查询天体位置、速度或姿态的数据/模型。历表结果必须说明目标、中心、参考系、时间尺度、覆盖区间和精度来源。

### 历表后端（Ephemeris Backend）
在历表接口接缝处提供具体数据或解析模型的适配器，例如 DE440 SPK 或 VSOP87。

### 相对状态（Relative State）

同一物理历元和参考轴下，目标相对中心的位置与速度。中心是结果的显式身份，不由参考轴暗示；位置是自由位移向量，不是绑定固定原点的空间点。


### 历表内核清单（Kernel Manifest）

按明确顺序冻结的一组历表内核。该顺序属于 snapshot 的规范语义；存在重叠覆盖时，后列内核优先于先列内核，不能由查询时的可变 priority 改写。

### 历表段（Ephemeris Segment）

内核中针对一个目标、中心、参考系和覆盖区间登记的一段数值表示。同一文件内的段顺序决定重叠时的优先级，查询必须按确定的遍历方向选择段。

### 覆盖区间（Coverage）
后端保证可查询的时间区间。区间外查询必须报错，不能静默外推。

### 天文事件（Event）
一个带时间区间和判据的根、极值、接触或状态转换结果。事件结果必须包含时刻、事件类型和可选接触阶段。

### 事件证据（Event Evidence）
支持一个事件时刻数值可信度的最终括区间、时刻不确定度、判据残差、迭代次数和求值次数。

### 二十四节气（24 Solar Terms）
太阳地心视黄经依次到达以春分点为 $0^\circ$、每隔 $15^\circ$ 的二十四个规定位置的时刻。节气名称、目标黄经、所用视黄经语义和民用时间偏移共同决定结果含义。

### 节气年（Solar-Term Year）
按一个固定 UTC 偏移选取的公历年内、依民用时间排序的二十四个节气事件；通常从小寒开始、以冬至结束。

### 升起、落下与地平可达性

升起和落下分别是目标中心高度相对一个显式地平判据的上穿与下穿。几何中心判据使用真空高度；折射后判据必须携带调用者提供的大气条件。搜索区间内始终高于判据返回拱极分类，始终低于判据返回永不升起分类；它们是成功结果，不是“未找到”错误。太阳上边缘、月球动态视半径和地形地平线属于其他显式判据，不能混入中心升落。

### 上中天与下中天

上中天和下中天分别由站心 CIRS 时角穿过 $0$ 与 $\pi$ 定义，不以采样高度的局部最大或最小替代。事件结果同时保留同历元站心位置与数值证据；极点、天顶和天底附近的方位无定义不使中天时刻失效。

### 晨昏蒙影（Twilight）

晨昏蒙影由太阳中心的站心真空高度上穿或下穿规定负高度定义：民用、航海、天文晨昏分别使用 $-6^\circ$、$-12^\circ$、$-18^\circ$。上穿为晨光始，下穿为昏影终。折射不重复并入这些负高度定义；搜索区间内太阳始终高于或低于阈值时返回连续明亮或连续黑暗分类。

### 合、冲、方照
由观测者所见天体黄经差或角距条件定义的配置事件。参考系、视位置模型和目标组合必须明确。

### 食
一个天体进入另一阴影或遮挡光源的接触序列。日食、月食和一般掩星共享几何原语，但采用不同观测者、阴影与接触定义。

### 贝塞尔根数
把日食局部几何投影到基本平面的时变参数集合；它是日食计算的中间表示，不是完整事件结果。

### 沙罗周期
食事件近似重复的分类周期。它用于系列归类和搜索种子，不替代精确历表计算。
